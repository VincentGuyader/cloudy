image: thinkr/runnerci
test:
  script:
  - R -e 'devtools::install_github("ropenscilabs/skimr")'
  - R CMD build . --no-build-vignettes --no-manual
  - PKG_FILE_NAME=$(ls -1t *.tar.gz | head -n 1)
  - R -e 'devtools::check()'
  - R -e 'devtools::test()'

